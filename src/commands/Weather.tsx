const Weather = () => {
    

    return <>I would make a request to a weather API here, but storing API key on Github Page's Secrets with React is actually not secure so I'm going to have to ask you to touch grass..</>
};

export default Weather